extension APICreds {
     static let emailValidation = APICreds(
          host: "{{ argument.validate_email_host }}",
          endpoint: "{{ argument.validate_email_endpoint }}",
          key: "{{ argument.validate_email_key }}"
     )

     static let magicalCats = APICreds(
          host: "{{ argument.cats_host }}",
          endpoint: "{{ argument.cats_endpoint }}",
          key: "{{ argument.cats_key }}"
     )
}